host:
  - "0.0.0.0"
port: 55000

https:
  enabled: yes
  key: "/var/ossec/etc/sslmanager/key.pem"
  cert: "/var/ossec/etc/sslmanager/cert.pem"
  use_ca: false

logs:
  level: "info"
  format: "plain"

drop_privileges: yes
